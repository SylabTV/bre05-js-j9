// vos imports ici
import { numbers } from "./numbers.js";
let i = 0
window.addEventListener("DOMContentLoaded", () => {
   // votre code ici
// calcule de la moyenne 
for(i<length++; i++){
   i=i+i
}

console.log(numbers);
export let computeAverage(numbers) = average
});

